[project]
name = "eprocessos-mock"
version = "0.1.0"
description = "Record/replay mock for the e-Processos REST API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "httpx>=0.27",
    "uvicorn[standard]>=0.34",
]

[project.optional-dependencies]
dev = [
    "ruff",
]
