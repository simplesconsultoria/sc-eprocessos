Added `tests/serializers/test_facade.py` covering the non-searchable / searchable facade serializer split and the `IExportImportRequestMarker` short-circuit; added `tests/serializers/test_base_item.py` covering the `BaseItemSerializer` transformer pipeline (`_transform_collections` with `image` / `url_foto` branches); extended `tests/utils/test_images.py` with parametrized coverage of `_local_proxy_url`, `process_image`, and `image_from_url_foto`; added `_form_config_title` checks to the Sessoes, Normas, and Materias content-type tests and refreshed the per-type fixtures. @ericof
