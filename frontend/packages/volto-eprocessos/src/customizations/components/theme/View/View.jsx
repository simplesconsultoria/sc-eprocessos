/**
 * Shim — points Volto's ``@plone/volto/components/theme/View/View`` import
 * at the addon-local View component (which forwards querystring filters to
 * ``getContent``).
 */

export { default } from '@simplesconsultoria/volto-eprocessos/components/View/View';
